# 投屏系统 · 开发手册

> 白皮书 / 技术文档  
> 版本：1.6  
> 最后更新：2026-08-08

---

## 一、项目概述

### 1.1 项目定位

Mac → iPad 局域网屏幕/窗口投屏系统。支持**多流并发**——多条独立流分别投不同窗口到不同端口，多条 iPad 可同时连接同一或不同流。

- Mac 端（MacCaster）：捕获屏幕/窗口，硬件编码，TCP 推流
- iPad 端（iPadReceiver）：TCP 接收，硬解码，全屏显示
- 支持两种编码模式：JPEG（保底）和 H.264（高清低延迟）
- 纯局域网 TCP 直连，无中转服务器

### 1.2 多流架构

```
ScreenStreamer（流管理器）
├── StreamSession #1 (port 8318)
│   ├── CastingServer (监听 8318)
│   ├── SCStream（捕获窗口 A）
│   └── VTCompressionSession（H264 编码）
├── StreamSession #2 (port 8319)
│   ├── CastingServer (监听 8319)
│   ├── SCStream（捕获应用 B）
│   └── CIContext（JPEG 编码）
└── StreamSession #3 (port 8320)
    ├── CastingServer (监听 8320)
    ├── SCStream（捕获显示器）
    └── VTCompressionSession（H264 编码）
```

每条流独立编码和广播。多台 iPad 连同一端口时自动取最高分辨率编码，低分辨率设备本地缩小。

### 1.2 技术栈

| 组件 | 技术 |
|------|------|
| Mac UI | SwiftUI + AppKit |
| Mac 屏幕捕获 | ScreenCaptureKit |
| Mac 编码 | VideoToolbox (H.264) / CIContext (JPEG) |
| iPad UI | SwiftUI + UIKit |
| iPad 解码 | VTDecompressionSession (H.264) / UIImage (JPEG) |
| 网络传输 | Network.framework (NWListener / NWConnection) TCP |
| 构建系统 | Xcode + xcodebuild |

---

## 二、通信协议

### 2.1 消息格式

每条消息：`[4 字节 payload 长度 big-endian] [1 字节 类型] [payload]`

### 2.2 消息类型

| 类型 | 方向 | 用途 |
|------|------|------|
| 0 | Mac→iPad | 编码配置 |
| 1 | Mac→iPad | 编码帧 |
| 2 | iPad→Mac | iPad 屏幕分辨率 |

### 2.3 type=0 编码配置

- 模式 1（JPEG，已废弃）：`codec=2`
- 模式 2（H.264 SPS/PPS，已废弃）：`codec=0 + 4B spsLen + sps + 4B ppsLen + pps`
- 模式 3（H.264 序列化格式描述，当前使用）：`codec=3 + 4B fdLen + serializedFormatDescription`

序列化格式描述由 `CMVideoFormatDescriptionCopyAsBigEndianImageDescriptionBlockBuffer` 生成。

### 2.4 type=1 编码帧

- JPEG 模式：payload = JPEG 文件数据（以 FF D8 开头）
- H.264 模式：payload = Annex-B 格式编码数据（以 00 00 00 01 起始码分割 NAL 单元）

### 2.5 type=2 iPad 分辨率

iPad 连接后自动发送，仅发送一次：
`[4B width big-endian] [4B height big-endian]`

宽高为 `UIScreen.main.bounds.size × UIScreen.main.scale`（物理像素）。

---

## 三、H.264 编解码链路

### 3.1 Mac 端编码

```
SCStream 捕获 → CVPixelBuffer (NV12)
  → VTCompressionSession (kCMVideoCodecType_H264, Main_AutoLevel)
  → CMSampleBuffer (AVCC 格式)
  → 转换为 Annex-B (起始码替换长度前缀)
  → 序列化格式描述 (BigEndianImageDescription)
  → TCP 发送
```

配置参数：
- 像素格式：kCVPixelFormatType_32BGRA（SCStream 捕获）
- VTCompressionSession 输入：BGRA（编码器内部色彩转换，质量优于外部 NV12）
- Profile: Main_AutoLevel
- RealTime: true
- AllowFrameReordering: false
- MaxKeyFrameInterval: 120
- ExpectedFrameRate: 60
- 默认码率：30Mbps（范围 8–100Mbps）

### 3.2 iPad 端解码

```
TCP 接收 → 解析消息
  → type=0: 反序列化 CMVideoFormatDescription
  → type=1: Annex-B → AVCC 转换
    → CMSampleBufferCreateReady
    → VTDecompressionSessionDecodeFrame
    → CVPixelBuffer → CIImage → CGImage → UIImage
    → UIImageView 显示
```

### 3.3 为什么不用 AVSampleBufferDisplayLayer

iOS 26 上 `CMVideoFormatDescriptionCreateFromH264ParameterSets` 和 `CreateFromHEVCParameterSets` 均返回 -12712（kCMFormatDescriptionBridgeError_InvalidParameter），无法从裸参数集重建格式描述。即使使用模拟器（宿主的 CoreMedia）创建成功，解码后也黑屏。

解决方案：Mac 端序列化完整的 CMVideoFormatDescription，iPad 端反序列化，绕过参数集重建。然后使用 VTDecompressionSession 手动解码 + UIImage 显示。

### 3.4 格式描述序列化

```swift
// Mac 端序列化
CMVideoFormatDescriptionCopyAsBigEndianImageDescriptionBlockBuffer(
    allocator: nil,
    videoFormatDescription: format,
    stringEncoding: 0,
    flavor: .isoFamily,
    blockBufferOut: &blockBuffer
)

// iPad 端反序列化
CMVideoFormatDescriptionCreateFromBigEndianImageDescriptionData(
    allocator: nil,
    bigEndianImageDescriptionData: pointer,
    size: data.count,
    stringEncoding: 0,
    flavor: .isoFamily,
    formatDescriptionOut: &desc
)
```

---

## 四、JPEG 编解码链路（保底方案）

### 4.1 Mac 端

```
SCStream 捕获 → CVPixelBuffer
  → CIImage → CIContext.jpegRepresentation (quality=0.92)
  → TCP 发送
```

### 4.2 iPad 端

```
TCP 接收 → UIImage(data:)
  → UIImageView 显示
```

---

## 五、iPad 分辨率自适应

1. iPad 连接时发送自身物理像素分辨率（type=2）
2. Mac 接收后存储在 `CastingServer.remoteScreenSize`
3. `ScreenStreamer.start()` 根据 iPad 分辨率设定编码尺寸
   - 按比例匹配 iPad 宽度或高度（取较大者）
   - 保留源画面宽高比
   - 如无 iPad 分辨率数据，默认上限 2732px

---

## 六、项目文件结构

```
投屏/
├── MacCaster/                        # Mac 发送端 Xcode 工程
│   └── MacCaster/
│       ├── ContentView.swift         # 主界面（窗口列表、设置）
│       ├── ScreenStreamer.swift      # 屏幕捕获 + 编码（JPEG & H.264）
│       ├── CastingServer.swift       # TCP 服务端 + 分辨率接收
│       ├── MacCasterApp.swift        # App 入口
│       └── Info.plist
│
├── iPadReceiver/                     # iPad 接收端 Xcode 工程
│   └── iPadReceiver/
│       ├── ContentView.swift         # 全屏视频 + 左滑设置面板
│       ├── H264Decoder.swift         # 解码器（JPEG + H.264 VTDecompressionSession）
│       ├── StreamClient.swift        # TCP 客户端 + 分辨率发送
│       ├── iPadReceiverApp.swift     # App 入口
│       └── Info.plist
│
├── 投屏·开发记录.MD
├── 投屏·开发手册.MD
├── 投屏·说明书.MD
└── README.md
```

---

## 八、批注系统

### 8.1 实现方案

批注采用 **CVPixelBuffer 直接绘制**方案（方案 B）：

1. `AnnotationEngine` 通过全局事件监听追踪鼠标在屏幕坐标系的轨迹
2. 每条流的 `drawAnnotations(on:)` 在编码前 lock 像素缓冲、创建 CGContext
3. 通过 `filter.contentRect × pointPixelScale` 计算坐标映射矩阵
4. 将屏幕坐标的笔触画入 BGRA 位图上下文
5. unlock 后正常编码

此方案使批注在任何投屏模式下均可见（窗口/应用/显示器），且不影响其他应用的正常操作。

### 8.2 性能

M 系列芯片：lock → draw → unlock 约 1-2ms，对 60fps 编码管线几乎无影响。

### 7.1 已知问题

- macOS 26 / iOS 26 上 `CMVideoFormatDescriptionCreateFromH264ParameterSets` 和 `CreateFromHEVCParameterSets` 返回 -12712
- 端口 8317 被僵尸进程占用时需换端口或重启
- VTDecompressionSession 解码后 CIImage→CGImage→UIImage 转换涉及 GPU→CPU 回读，有轻微性能开销
- SwiftUI 全屏视频 + 设置面板的滑动手势偶有冲突

### 7.2 改进方向

- Metal 直接渲染解码后的 CVPixelBuffer（避免 GPU→CPU→GPU 回读）
- 支持 HEVC 硬件解码（等 iOS CoreMedia 修复 -12712）
- 支持音频同步传输
- 多 iPad 同时投屏
- 支持远程广域网投屏（需 STUN/TURN）
