# 投屏 · 诊断日志

> 运行时日志输出位置与格式说明

---

## 日志位置

```
/tmp/MacCasterLogs/diagnostic.log
```

MacCaster 启动后自动创建。可通过以下命令实时查看：

```bash
tail -f /tmp/MacCasterLogs/diagnostic.log
```

或复制到项目目录：

```bash
cp /tmp/MacCasterLogs/diagnostic.log ./diag-output/
```

---

## 日志标签

| 标签 | 模块 | 内容 |
|------|------|------|
| `Annotation` | 批注引擎 | 工具栏显示/隐藏、笔/橡皮切换、清空、窗口创建/关闭、全局监听启停 |
| `Stream` | 数据流 | 批注开关、刷新、流启停 |

---

## 重要时间点

| 步骤 | 预期日志 |
|------|---------|
| 点批注按钮（开启）| `[Annotation] showToolbar` → `startGlobalMonitor` → `toolbar built` → `drawingWindow built` |
| 点笔按钮 | `[Annotation] togglePen → pen` |
| 拖动画线 | （无新日志，仅绘图） |
| 点橡皮按钮 | `[Annotation] toggleEraser → eraser` |
| 点清空按钮 | `[Annotation] clearAll strokes=0` |
| 点批注按钮（关闭）| `[Annotation] hideToolbar begin` → `stopGlobalMonitor` → `closing drawingWindow` → `drawingWindow closed` → `closing toolbarWindow` → `toolbarWindow closed` → `hideToolbar end` |

---

## 异常排查

若关闭批注时死机，查看最后一行日志：

- 卡在 `stopGlobalMonitor` → 事件监听移除异常
- 卡在 `closing drawingWindow` → 全屏绘图层关闭异常
- 卡在 `closing toolbarWindow` → 工具栏窗口关闭异常
- 未出现 `hideToolbar end` → 流程中断未完成
