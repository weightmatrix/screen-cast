# 投屏 · 开发记录

> 开发日志 / 变更历史  
> 每次修改代码后进行维护

---

## #001 — 2026-08-06 项目初始化与基础链路搭建

### 操作

- 创建 MacCaster（macOS 投屏发送端）和 iPadReceiver（iPad 接收端）两个 Xcode 项目
- 实现 Mac 端 ScreenCaptureKit 窗口/显示器捕获
- 实现 H.264 VideoToolbox 硬件编码 + Annex-B 格式转换
- 实现 TCP 协议：[4 字节长度][1 字节类型][payload]
  - type=0：编码配置（SPS + PPS）
  - type=1：编码帧数据
- 实现 iPad 端 NWConnection TCP 接收 + AVSampleBufferDisplayLayer 解码显示
- 技术栈：SwiftUI + VideoToolbox + ScreenCaptureKit + Network.framework

### 决策

- 确定投屏链路：Mac 捕获→H.264 硬件编码→TCP→iPad 接收→AVSampleBufferDisplayLayer 解码显示
- 确定协议：长度前缀 + 类型字节，简单可靠
- 确定部署目标：macOS 15+ / iOS 15+
- 默认端口 8317

---

## #002 — 2026-08-06 TCC 屏幕录制权限修复

### 问题

MacCaster 启动后无法获取屏幕录制权限，SCShareableContent 返回空。

### 修复

- `project.pbxproj` 中 `CODE_SIGN_IDENTITY` 从 `"-"`（ad-hoc）改为 `"Apple Development"`
- 执行 `tccutil reset ScreenCapture com.witten.touping.maccaster`
- 需要 Apple Development 签名才能通过 TCC 权限检查

---

## #003 — 2026-08-06 H.264 黑屏问题诊断

### 问题

iPad 端已连接、帧计数不断增长，但画面始终黑屏。

### 诊断过程

- 确认 Mac 端编码和发送正常（probe.py 抓包验证 SPS/PPS/帧数据）
- 模拟器日志：`enqueued #7500 pts=7499 size=59 dims=1280x664 drop=0 err=none`
- FigVideoQueue 显示 338 帧入队但未渲染——解码器静默失败
- 根因定位：macOS 26 VideoToolbox H.264 编码器产出的参数集（11 字节无 VUI 的 SPS），被 macOS 自身的 `CMVideoFormatDescriptionCreateFromH264ParameterSets` 拒绝（返回 -12712）
- 真机 iOS 26 同样返回 -12712——该 API 在 iOS 26 上疑似整体损坏

### 尝试的修复（均失败）

- 编码器 Profile 从 AutoLevel 改为 Main_4_0——参数集仍被拒绝
- 手工合成 VUI 注入——SPS 仍被拒绝（位错误风险）
- 使用 HEVC 替代——`CMVideoFormatDescriptionCreateFromHEVCParameterSets` 同样返回 -12712

---

## #004 — 2026-08-06 MJPEG 过渡方案

### 决策

H.264/HEVC 参数集创建 API 在 iOS 26 上整体损坏，先用 MJPEG 保证能出画面。

### 实现

- Mac 端：CIContext JPEG 编码（硬件加速），quality=0.8
- iPad 端：UIImage(data:) 直接解码，UIImageView 显示
- 协议简化：只发 type=1 JPEG 数据，无配置消息

### 结果

画面出现，但清晰度不高（JPEG 有损）。作为保底方案保留。

---

## #005 — 2026-08-06 H.264 格式描述序列化方案

### 思路

根本问题：iOS 无法从裸 SPS/PPS 重建 CMVideoFormatDescription。  
解决方案：在 Mac 端序列化完整的 CMVideoFormatDescription，传到 iPad 反序列化还原。

### 实现

- Mac 端：`CMVideoFormatDescriptionCopyAsBigEndianImageDescriptionBlockBuffer` 序列化
- iPad 端：`CMVideoFormatDescriptionCreateFromBigEndianImageDescriptionData` 反序列化
- 协议：type=0 codec=3 + 序列化 FD 数据
- 使用 AVSampleBufferDisplayLayer 显示——帧入队成功但黑屏（与之前相同）

---

## #006 — 2026-08-06 H.264 VTDecompressionSession 方案

### 思路

放弃 AVSampleBufferDisplayLayer，改用 VTDecompressionSession 手动解码 + UIImage 显示。

### 实现

- iPad 端：用反序列化出的完整 CMVideoFormatDescription 创建 VTDecompressionSession
- 输入：Annex-B→AVCC 转换后的 CMSampleBuffer
- 输出：解码后的 CVPixelBuffer→CIImage→CGImage→UIImage
- 显示：UIImageView（UIViewRepresentable + FitView 容器自动布局）

### 结果

**画面成功出现！** H.264 硬解在真机 iOS 26 上正常工作，画质流畅。

---

## #007 — 2026-08-06 界面重设计

### 改动

- iPad 端改为全屏视频 + 左滑设置面板（抽屉式）
  - 从屏幕左边向右滑动拉出设置面板
  - 点击视频区域或反向滑动关闭面板
  - 设置面板显示：IP、端口、连接按钮、诊断文字、帧数
- SwiftUI Image 闪动修复：改用 UIViewRepresentable + 回调直接更新 UIImageView
- 容器 FitView 在 `layoutSubviews` 中自动调整 imageView.frame

---

## #008 — 2026-08-06 iPad 分辨率自适应

### 改动

- iPad 连接时自动发送屏幕物理像素（UIScreen.bounds × scale）给 Mac
- 协议新增：type=2，payload=8 字节（width + height，uint32 big-endian）
- Mac 端 CastingServer 接收并存储 `remoteScreenSize`
- ScreenStreamer 根据 iPad 分辨率设定编码尺寸（保留源画面比例）
- 编码上限从固定 2732px 改为 iPad 实际分辨率

### 画质优化

- JPEG quality：0.85 → 0.92
- H.264 默认码率：12Mbps → 20Mbps，最高 50Mbps
- 帧率：60fps
- 最大关键帧间隔：120 帧

---

## #010 — 2026-08-08 2K 锐度提升

### 改动

- SCStream 像素格式：420v → BGRA（消除色度二次采样损失）
- VTCompressionSession 输入：NV12 → BGRA（让编码器自己做色彩转换，质量更高）
- H.264 默认码率：20Mbps → 30Mbps
- 码率上限：50Mbps → 100Mbps
- JPEG quality：保持 0.92（JPEG 在高质量下走 4:4:4，颜色无损）

### 原理

SCStream 捕获 NV12（4:2:0）时，系统直接对原始画面做色度降采样，低质且不可逆。
改 BGRA 后捕获完整 4:4:4，VTC 编码器用自己的算法转为 4:2:0，对文字 UI 的锐边保留更好。
高码率弥补放大后的压缩损失。

### 操作

- 创建《投屏·开发记录.MD》
- 创建《投屏·开发手册.MD》
- 创建《投屏·说明书.MD》
- 初始化 Git 仓库，创建 .gitignore
- 创建 GitHub 仓库并推送
