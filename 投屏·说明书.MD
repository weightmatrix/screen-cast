# 投屏系统 · 使用说明书

> 用户手册  
> 版本：1.2

---

## 1 系统要求

| | Mac（发送端） | iPad（接收端） |
|---|---|---|
| 操作系统 | macOS 15+ | iOS 15+ |
| 硬件 | Apple Silicon（M 系列） | iPad Air / Pro / mini |
| 网络 | 与 iPad 同一局域网 WiFi | 与 Mac 同一局域网 WiFi |
| 权限 | 屏幕录制（系统设置→隐私） | 无特殊权限 |

---

## 2 安装

### 2.1 Mac 端

1. 用 Xcode 打开 `MacCaster/MacCaster.xcodeproj`
2. 确认签名：Target → Signing & Capabilities → Team 选 `Apple Development`
3. 按 **⌘R** 编译运行

首次运行会弹出屏幕录制权限请求，必须**允许**。

### 2.2 iPad 端

1. 用 Xcode 打开 `iPadReceiver/iPadReceiver.xcodeproj`
2. 选择你的 iPad 设备（真机）
3. 按 **⌘R** 编译安装到 iPad
4. iPad 上首次打开需在 设置→通用→VPN与设备管理 中**信任开发者**

---

## 3 使用步骤

### 3.1 启动投屏（多流支持）

1. **Mac 端**：启动后点击 **"+ 添加投屏流"** 按钮
2. 在弹出窗口中：
   - 选择投屏内容（显示器 / 应用 / 窗口）
   - 设置编码模式：H264 或 JPEG
   - 设定码率（8–100 Mbps）、FPS（30/60）、光标显示
3. 点击"开始投屏"
4. 重复以上步骤可添加多条流，每条流有独立端口（8318、8319...）
5. 主界面显示所有流的实时状态（FPS、设备数、编码分辨率）
6. 每条流可独立停止、切换光标显示
7. **iPad 端**：打开 App，输入对应流的端口号连接

### 3.2 选择编码模式

在 Mac 端投屏状态下：
- **关闭"H264 编码"开关**：使用 JPEG 模式（保底，兼容性最好）
- **打开"H264 编码"开关**：使用 H.264 编码（画质更好，延迟更低）

H.264 模式下可调节码率（8–100 Mbps），推荐 30–60 Mbps 获得 2K 级别锐度。

### 3.3 iPad 界面操作

- **全屏显示**：画面自动填满 iPad 屏幕
- **设置面板**：从屏幕左边缘向右滑动，拉出设置面板
  - 修改 Mac IP 地址和端口
  - 连接 / 断开
  - 查看诊断信息（编码格式、分辨率、帧数）
- **关闭面板**：点击视频区域或再次向左滑动

### 3.4 停止投屏

- **Mac 端**：点击"停止投屏"按钮
- **iPad 端**：点"断开"按钮或关闭 App

---

## 4 多设备与多分辨率

- **多台 iPad 连同一流**：在各自 iPad 输入相同 IP + 端口即可。Mac 自动取所有已连接 iPad 的最高分辨率编码，低分辨率 iPad 本地缩小显示，都不糊。
- **多台 iPad 连不同流**：在添加流时选择不同内容，iPad 分别输入对应端口号。
- **光标控制**：每条流可独立开关鼠标光标显示（Mac 界面每条流的"光标"开关）。

---

## 5 常见问题

### 4.1 iPad 连接不上

- 确认 Mac 和 iPad 连接同一 WiFi
- 检查 Mac IP 是否正确（MacCaster 界面会显示）
- 检查防火墙：Mac 系统设置→网络→防火墙→关闭或允许传入连接
- 检查端口是否被占用（换一个端口如 8319）

### 4.2 画面黑屏 / 无画面

- 确认 Mac 端已选择窗口并开始投屏
- 检查 iPad 诊断文字（左滑打开设置面板）：
  - "H264 OK 1920x1080" → 编码链路正常
  - "H264 帧:N" → 帧在正常接收解码
  - 如显示错误码，记录并反馈

### 4.3 画面模糊

- 尝试切换到 H.264 模式并提高码率（推荐 20–30 Mbps）
- 如果是 JPEG 模式，画面质量受限于 JPEG 有损压缩
- 确认投屏窗口没有被缩小到很小

### 4.4 画面卡顿

- 降低码率（如从 30 Mbps 降到 12 Mbps）
- 确保 WiFi 信号良好
- 检查 Mac CPU 占用（ScreenCaptureKit 有一定开销）

### 4.5 MacCaster 窗口不显示可投屏的窗口

- 现象：窗口列表为空
- 原因：屏幕录制权限未授予
- 解决：系统设置→隐私与安全性→屏幕录制→确保 MacCaster 已勾选
- 如已勾选但仍无效，在终端执行：
  ```
  tccutil reset ScreenCapture com.witten.touping.maccaster
  ```
  然后重新启动 MacCaster，会再次弹出权限请求

### 4.6 MacCaster 启动失败 / 端口占用

如错误信息提示端口已被占用（如 8317），可能 Xcode 前次调试进程未完全退出：
- 在 Xcode 里按 **⌘.** 停止
- 打开活动监视器搜索 MacCaster 并强制退出
- 或在终端执行：
  ```
  kill -9 $(pgrep MacCaster)
  ```
- 重新按 ⌘R 启动
- 如果端口持续被占用，可在代码中将端口临时改为 8318（同步修改 iPad 端）

---

## 5 iPad 端默认配置

| 项目 | 默认值 |
|------|--------|
| Mac IP | （首次需手动输入） |
| 端口 | 8318 |
| 自动连接 | 是（App 启动时若已填写 IP 则自动连接） |
| 屏幕常亮 | 是（连接后自动禁用自动锁屏） |
